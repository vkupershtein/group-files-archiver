[project]
name = "group-files-archiver"
version = "0.1.0"
description = "Tool to archive files of user group members"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "tqdm>=4.30.0",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.4.6",
    "pytest>=8.4.1",
]